# Caching

{py:func}`~minisim.simulate_cached` memoizes recordings to disk, keyed by
`spec.cache_key()`. These helpers locate that cache.

## simulate_cached

```{autofunction} minisim.simulate_cached
```

## cache_dir

```{autofunction} minisim.cache_dir
```

## cache_path

```{autofunction} minisim.cache_path
```
